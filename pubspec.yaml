name: DividendMine
description: Monthly dividend control.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.0.0
  sqflite: ^2.0.0-nullsafety.2
  path_provider: ^2.0.1
  flutter_state_notifier: ^0.7.0
  flutter_masked_text: ^0.7.0
  intl: "^0.16.1"
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
  uses-material-design: true

